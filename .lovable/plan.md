

# Increase Logo Size in Quotation Email

## Change

**File: `supabase/functions/send-quotation/index.ts`**

Two logo `<img>` tags need larger heights:

1. **Header logo** (currently `height: 100px`) → increase to `height: 140px`
2. **Footer logo** (currently `height: 60px`) → increase to `height: 90px`

No other files affected.

